<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kakao">

	<insert id="insertKakaoPay" parameterType="wh.admin.manage.model.PayListDto" >
		insert into pay_list values(#{member_id},#{purchase_amount},#{fork},now())
	</insert>
	
	<update id="updateFork" parameterType="wh.user.home.model.HomePageMemberDto">
		update members set fork = fork+#{fork} where member_id = #{member_id}
	</update>
</mapper>