<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin">

	<select id="listAll" resultType="yh.admin.controller.AdminFaqDto">
		select * from FAQ_user2 order by
		bno desc

	</select>

	<insert id="insert" parameterType="yh.admin.controller.AdminFaqDto">
		insert into FAQ_user2 values(
		#{bno},
		#{title}, #{content},#{menu},#{viewcnt}
		)
	</insert>

	<select id="view" resultType="yh.admin.controller.AdminFaqDto">
		select * from FAQ_user2 where bno =
		#{bno}
	</select>

	<update id="increaseViewcnt" parameterType="int">
		update FAQ_user2 set
		viewcnt = viewcnt +1 where bno = #{bno}
	</update>

	<update id="updateArticle">
		update FAQ_user2 set title = #{title}, content =
		#{content},menu = #{menu} where bno = #{bno}
	</update>

	<delete id="deleteArticle">
		delete from FAQ_user2 where bno = #{bno}
	</delete>

<!-- 	<select id="pageList" resultType="custom.controller.BoardDto"
		parameterType="custom.controller.Criteria">
		select * from FAQ_user order by
		bno desc, regdate desc
		limit #{page}, #{perPageNum}
	</select>
	<select id="countBoardList" resultType="int" parameterType="custom.controller.BoardDto" >
		select count(*) from FAQ_user where bno = #{bno}
	</select> -->

</mapper>